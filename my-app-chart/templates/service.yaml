apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-app-service
spec:
  selector:
    app: {{ .Release.Name }}-app
  ports:
  - name: web
    port: 80
    targetPort: {{ .Values.app.containerPort }}
  type: NodePort
